"""AI Bridge for Ableton Live — host side (runs outside Live).

The client (`client.py`), ergonomic API (`api.py`), beyond-LOM tools
(`render.py`, `winui.py`, `audio_analysis.py`), and the MCP server
(`mcp_server.py`) that talk to the remote script over 127.0.0.1:8766.
"""
